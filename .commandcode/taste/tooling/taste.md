# Taste

## Environment & tooling
- Works on Windows: use the PowerShell tool for git/shell commands; plain shell commands with `;`/`&` separators get mangled and should be avoided. Confidence: 0.9
- Docker is only available inside WSL (Debian distro), not on the Windows PATH — all docker operations go through `wsl -d Debian -- bash -lc "..."` wrappers; long docker builds should run as a background task with output tee'd to a log inside WSL (`/tmp/...`) because Windows-side capture buffers and looks empty; when handing over a running app, report the port verified reachable from the Windows host side (e.g. `http://127.0.0.1:3001`), not just inside WSL/the container. Confidence: 0.85
- `uv`-based Python helper scripts (e.g. BMad memlog/customization resolvers) are not available on this machine — fall back to manual equivalents rather than blocking on them. Confidence: 0.7
- Git fork workflow: keeps a clean `production` branch as an exact mirror of `main` for contributing upstream, and prefers GitHub-side toggles (default branch, disabling CI Actions) to be done by the user rather than via CLI. Confidence: 0.6
- Prefers sizable feature work on a dedicated branch (e.g. `feat/<name>`) cut from `main`, keeping `main` clean and uncommitted-to during implementation. Confidence: 0.6
- For its own deployment/operational work (dockerization, Makefile, agent tooling dirs), expects the agent to execute the git release steps itself on terse instruction ("commit and push and merge to production") — merging directly onto the `production` branch rather than routing local deployment work through a PR, while leaving upstream contribution branches (and their PRs) untouched. Confidence: 0.5
- This machine's npm global config sets `omit=dev`, so `npm install` never installs devDependencies — use `npm install --include=dev --ignore-scripts` (the husky `prepare` script otherwise fails and aborts the install); as a result test/typecheck gates (vitest, @types) are unavailable until deps are installed. Confidence: 0.9
- Installing with `--ignore-scripts` skips native-module builds — after installing, run `npm rebuild better-sqlite3 node-pty` (plus `node scripts/fix-node-pty.js`) or the server fails to boot on missing `better_sqlite3.node`. Confidence: 0.7
- The repo's oxlint config enforces import boundaries via explicit element allowlists in `.oxlintrc.json`: any new file under `server/shared/` (e.g. `cursor-cli-path.ts`) must be added to the `backend-shared-utils` element pattern, or every importer fails `boundaries(no-unknown)` and Jenkins' Lint stage fails. After such a fix, keep both the fork `production` branch and any upstream PR branch green. Confidence: 0.75
- Wants the app runnable/deployable via Docker Compose wrapped in a Makefile with the conventional targets `make up`, `make down`, `make build`, `make clean` (mirroring compose verbs), and expects the dockerization to be "proper" (idiomatic multi-stage build, healthcheck, persisted data volume, `.dockerignore`/`.env.example`) rather than a minimal one-off. Confidence: 0.7
- Will not let official/upstream-managed files be modified (denied writes into the repo's official `docker/` sandbox templates); prefers additive sidecar files — a root `Dockerfile`, a separate custom compose file, and a Makefile that targets that custom compose explicitly (`docker compose -f compose.yaml`) — keeping official files untouched. Confidence: 0.75
- Treats automated PR-review bot findings (CodeRabbit) as a real quality gate on their open PRs: when CodeRabbit rates a PR "high merge risk", expects the agent to pull up the review comments and push code fixes addressing each flagged issue (rather than ignoring or waiving them), then push so the bot re-reviews. Confidence: 0.6
- Treats the fork's `production` deployment branch and upstream PRs as two separate tracks that must BOTH carry a fix: when a clean upstream PR is opened for a code change, still expects/checks that the same change is present on `production` ("did you merge it in production branch?") — and expects an answer backed by evidence (commit SHAs on local + origin/production, published image digest), not by the PR existing alone. Terse instructions explicitly name both tracks in one breath ("commit and push to production ... if you think then create another PR"), so the agent should push to `production` first and then open the upstream PR as a parallel deliverable. Even after a detailed handover summary he independently double-checks the deliverable with a terse state probe ("in production branch commit and pushed ?") — the answer must be verified live from `git log origin/production` (SHA), `git branch --show-current`, and `git status`, not restated from the summary; incidental untracked files (e.g. `.commandcode/taste/*`) are expected to be reported as intentionally-never-committed rather than treated as a dirty tree. Confidence: 0.72
- Maintains a fork of an upstream repo (`claudecodeui-contrib`) where the `production` branch holds local deployment assets (Dockerfile, compose.yaml, Makefile, CasaOS manifest) and is kept in sync with origin; when that branch has advanced from other sessions/processes (merges, Jenkinsfile), expects the agent to detect it, reconcile (stash conflicting untracked agent-tooling files to switch branches), and land new deployment work directly on the up-to-date `production` — not on stale feature branches. Confidence: 0.7
- CasaOS manifest work: prefers a single persistent bind mount at `/data` that captures ALL app state (auth.db, settings, assets, per-provider CLI state under `$HOME`) — if needed, alters the Dockerfile (e.g. `HOME=/data`) so the one mount backs everything, and validates the running container actually writes all state under that dir before committing. Confidence: 0.8
- CI/CD runs on a containerized Jenkins (jenkins.cloud.braintechsolution.com) where the Jenkins host itself has no node binary — node-dependent pipeline stages must run inside the node Docker image (`docker run --rm node:22-bookworm-slim` with the workspace bind-mounted), never as bare `node -p` on the host (exit-127 trap); pushes to the `production` branch auto-trigger new builds via webhook. Confidence: 0.7
- Extends the mirror-a-reference-file pattern to CI: when asked to "add a jenkins file like this one" he points at a reference Jenkinsfile in another of his repos (agent-zero-contrib) and expects the agent to fetch it and mirror its structure and comment style adapted to this repo — declarative pipeline with unattended install/lint/typecheck/build gates on every push run inside throwaway `node:22-bookworm-slim` containers (never bare host node, since the containerized Jenkins host has no node), a "Resolve host workspace" stage bridging the Jenkins-container path to the Docker-host path, and a production-only Docker Hub push gated behind a human `input()` approval whose timeout skips the push instead of failing the build. Confidence: 0.7
- Keeps a consistent CasaOS app-store manifest pattern across all his fork repos (agent-zero, OpenHands, CloudCLI, ...) and, on a terse "make it casaos compatible" with reference URLs, expects the agent to fetch those manifests and mirror their exact structure — image from his Docker Hub namespace tagged `:production`, `x-casaos` metadata block with raw-GitHub icon, single `/DATA/AppData/<app>/data` bind mount with `create_host_path: true` (including making the image persist all app state under that one mounted dir), an offset host port (~50081) and a node-based healthcheck — rather than inventing a new layout. Confidence: 0.8
- For CasaOS manifests meant to be copy-imported, wants a genuinely zero-config "copy the yml, import it in CasaOS, and it just runs" experience — not merely a schema-correct file: the image repo/tag must actually exist on Docker Hub (a 404 repo silently breaks the import), the icon URL must resolve, and the container must be able to write to its bind-mounted data dir on first run; when a non-root runtime user (uid 1000) cannot write to a root-owned `/DATA/AppData/<app>` bind mount, accepts running the container as root so host-ownership never blocks state writes — and expects the agent to validate all of it end-to-end (docker compose config, manifest inspect, live container write test) before committing/pushing. Confidence: 0.7
- For publishing app images, prefers a fast local build in WSL (`docker compose build` from the current `production` branch) and direct `docker push` to Docker Hub over routing through the slow containerized Jenkins pipeline — said explicitly while a Jenkins run was in flight ("i want you to build locally in wsl then push to docker hub"); expects the agent to drive build → tag → push → registry-verify end-to-end and report the resulting digest/tags. Confidence: 0.7
- Docker release builds for this repo must be forced with `--no-cache` after any source merge: BuildKit's layer cache reused the pre-merge `COPY . .` layer even though the merge changed 52 source files, silently shipping a stale image without the new code; a `CACHED` `COPY . .` line in the build log means the merged source was never compiled (the Jenkinsfile's app-image stage builds `--no-cache` for the same reason). Confidence: 0.75
- For AI-CLI provisioning in his CasaOS fork containers, settled on a two-variant distribution rather than one forced design: an isolated default manifest (CLIs bundled in the image; auth via API-key env fields or one-time interactive `/login` persisted under the `/data` mount; runs as root for root-owned `/DATA` paths) PLUS a shared variant that reuses the host machine's existing logins — bind-mounting host dotdirs `~/.claude`, `~/.commandcode`, `~/.codex`, `~/.cursor` into `/data/.xxx` and running as the host uid (1000) so file ownership matches. He personally wants this shared variant on his VPS to reuse his host claude/command-code/cursor logins rather than re-authenticating inside the container, accepting that the bundled CLIs are different versions sharing the same state dirs. Sharing host binaries (nvm/`~/.local`-installed CLIs) remains disfavored — only auth/config state dirs are mounted. On the VPS the shared variant did auto-inherit the host Claude login, and when a provider misses (cursor, command-code) he reports it tersely and asks why — expecting a per-provider root-cause (missing bundled binary vs. version/credential-format mismatch between the host and the container's bundled CLI) established by probing the running container, not acceptance of partial success; when the container's own CLI reports "Authenticated" but the UI shows not connected, expects the agent to reconcile that by checking the auth provider's `status --json` parsing against the CLI's real output and the mounted credential file. Confidence: 0.8
- Auth-sharing mechanics for the shared CasaOS variant (established by probing the real image + API in WSL): the app's auth providers honor API-key env vars first (`ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` → Claude, `OPENAI_API_KEY` → Codex) so those need zero interactive login when keys are set; Claude OAuth lives in `~/.claude/.credentials.json`, Codex in `~/.codex/auth.json`, and Command Code status is read ONLY by running the bundled CLI's `command-code status --json` (checks `~/.commandcode/auth.json`, a versioned private format, so a container-bundled v1.47 CLI can report unauthenticated against a v1.39 host credential even though `docker exec` inside the container shows authenticated); Cursor's auth is machine-bound/desktop-app-stored and does NOT transfer via file mounts — it needs a one-time in-container `agent login` (browser device flow); provider auth status is fetched fresh per UI request with no server-side caching, so "CLI says authenticated but UI says not connected" after a restart is a stale server/UI state issue — recreate the container (not just restart) and hard-refresh on the right port. Confidence: 0.8
- For conflicting deployment trade-offs he can't resolve himself, he explicitly asks the agent to split the deliverable into two clean variant files (one per use case, e.g. isolated vs. shared-host-logins) with shared structure — the primary manifest stays the clean default and the variant is a companion file with a distinct `container_name`, store id, title and port so both can run side-by-side — rather than one config cluttered with optional mount blocks/comment toggles. Confidence: 0.7
- When a container's default runtime user (root) would clash with bind-mounted host-user-owned state (dotdirs under a real `/home/<user>`), expects the container to run as the matching host UID (`user: "1000:1000"`, verified via `id -u <user>`) so container writes keep the host's ownership — and the shared variant's app-data mount must point at a host-user-owned path (e.g. `~/.cloudcli`, pre-created + `chown`ed to that uid), never a root-owned `/DATA` dir. The root-owned `/DATA` flow keeps root. Confidence: 0.6
- For CasaOS/VPS deployment-config work, prefers the agent to reproduce and verify behavior locally in WSL (throwaway containers with the real image, host-shaped dotdirs, real API endpoints) and only then finalize the shared manifest — instead of repeatedly probing the live VPS container or handing the user diagnostic command lists to run on the VPS. The local verification must be ONE fresh, complete, automatic pass through the app's own API checking every provider ("look i told you to run wsl local fresh ly then check automaitaclly all the agent get by cloudcli ui or not"), with whatever fails fixed before handover ("if not then fix then handover to me"). Expected even when the local WSL box lacks the exact host CLIs (simulate them with structurally-correct auth files and test through the real provider auth/status endpoints). Confidence: 0.7
- CasaOS/app-store manifests he distributes must be username-independent: a hardcoded host user (e.g. `/home/asiq/...` bind mounts) is a portability bug that breaks the file for anyone else who imports it — he flags it and expects it removed. Docker bind sources are literal host paths and cannot auto-detect the logged-in user; since CasaOS launches containers as root, `${HOME}` interpolation would resolve to `/root` (the reason official CasaOS apps mount `/DATA/AppData/$AppID`, never host-home paths). The endorsed resolution: delete every `/home/<user>` mount, keep all state under the single `/DATA/AppData/<app>` folder, retain only `user: "1000:1000"` (typical first host user) plus a documented one-time `chown` (e.g. `sudo mkdir -p ... && sudo chown -R 1000:1000`), and route host-CLI sharing host-side (point host CLIs' config dirs at the shared folder / symlinks) rather than via container mounts — no username string anywhere in the manifest, verified by grep for remnants before committing. Confidence: 0.6
- Self-hosted web apps on the VPS (e.g. the CloudCLI UI on its ~50081/50082 port) are reached via subdomains fronted by an external Nginx reverse proxy he manages himself — NPMplus (Nginx Proxy Manager plus), whose admin UI sits at its own `npm.*` subdomain — not CasaOS's built-in app proxy. When diagnosing UI errors like "Failed to fetch" on long-running requests from behind that proxy, treat the nginx hop as a likely cut point: nginx's default 60s read/send timeouts kill long POSTs (e.g. plugin install = git clone + `npm install`, which completes fine in seconds-minutes on the backend with no client-side timeout/AbortController), surfacing as a browser-level fetch failure even though the server succeeded; the fix is raised `proxy_read_timeout`/`proxy_send_timeout` on the CloudCLI location, and the direct `http://<vps-ip>:<port>` route is the bypass test that isolates the proxy. Confidence: 0.6
oxied subdomain), hands over the real URLs — the app subdomain plus the NPMplus admin subdomain — with a terse "open browser check yourself", expecting the agent to drive its own headed browser session against the live sites (agent-browser open/snapshot, curl response headers) to reproduce and inspect the layer in question, rather than continuing multiple-choice Q&A or only probing the backend locally in WSL; when the live page then requires login, the agent pauses and asks him to authenticate in the headed browser (agent never asks for the password). Confidence: 0.55
- Whenever the user is expected to authenticate in the agent's browser session, the session must actually be launched visibly headed (`--headed` open of the login URL) — a window that exists only in the daemon/headless state is useless to him; he flags it tersely ("i am not seeing the browser") and expects the agent to check session health (e.g. `agent-browser ... doctor --quick --offline`), close the stale session, and relaunch it `--headed` on the login page so he can complete the login — not to assume he logged in or proceed without him. Confidence: 0.6
